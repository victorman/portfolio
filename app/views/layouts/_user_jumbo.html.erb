<div class="jumbotron">
	<h1><%= @user.name %></h1>
	<p><%= @user.title %></p>
	<p><%= @user.location %></p>
	
	<% if admin? %>
	<div class="pull-right">
		<%= link_to settings_path do %>
			<span class="glyphicon glyphicon-pencil" aria-hidden="true" aria-label="User Settings"></span>
		<% end%>
	</div>
	<% end %>
</div>